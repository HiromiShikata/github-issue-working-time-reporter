describe('OctkitCardRepository', () => {
  describe('findByCardNumber', () => {
    test.todo('success');
    test.todo('run with invalid repositoryName');
    test.todo('run with invalid cardNumber');
  });
});
